enum ROUTES {
  HOME = "/",
  QUOTES = "/quotes",
  QUOTE = "/quote/:id",
  LEADS = "/leads",
  TOURS = "/tours",
  INVOICES = "/invoices",
  ANALYTICS = "/analytics",
  TEAM = "/team",
  ADMIN = "/admin",
  SUPPORT = "/support",
}

export default ROUTES
